# undo

Undo the last commit, keeping changes staged.

**Alias:** `devflow u`

## Description

Shows a preview of the commit that will be undone before confirming. The changes from the undone commit remain staged so you can re-commit or modify them.

## Options

| Option | Description |
|--------|-------------|
| `--dry-run` | Preview which commit would be undone without executing |
